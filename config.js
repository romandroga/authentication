const serverName = 'localhost'


const config = {
  PORT: 3001,
  user: "root",
  host: "localhost",
  password: "password",
  database: "authentication",
}

// if(serverName === 'heroku'){
//
// }



module.exports = config
